{
	"name": "messenger",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"packageManager": "npm@10.9.2",
	"scripts": {
		"dev": "buf generate && turbo run dev",
		"format": "prettier --write .",
		"lint": "turbo run lint",
		"build": "buf generate && turbo run build",
		"preview": "npm run build && turbo run preview",
		"prepare": "husky && npm run lint && npm run format",
		"pre-commit": "npm run lint && npm run format"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"workspaces": [
		"backend",
		"frontend"
	],
	"devDependencies": {
		"@bufbuild/buf": "^1.54.0",
		"@bufbuild/protoc-gen-es": "^2.5.2",
		"husky": "^9.1.7",
		"prettier": "^3.6.2",
		"turbo": "^2.5.6"
	}
}
